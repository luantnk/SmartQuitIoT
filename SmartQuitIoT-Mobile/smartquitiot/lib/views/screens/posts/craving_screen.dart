import 'package:flutter/material.dart';

class CravingScreen extends StatelessWidget {
  const CravingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Craving Screen')));
  }
}
